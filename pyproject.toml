[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rlinf"
version = "0.4.0"
authors = [{ name = "RLinf Contributors" }]
description = "Reinforcement Learning Infrastructure for Embodied and Agentic AI"
readme = {file = "README.md", content-type = "text/markdown"}
requires-python = ">=3.10"
license = {file = "LICENSE"}
keywords = [
    "reinforcement-learning",
    "embodied-intelligence",
    "large-language-models",
]
classifiers = [
    #   2 - Pre-Alpha
    #   3 - Alpha
    #   4 - Beta
    #   5 - Production/Stable
    "Development Status :: 2 - Pre-Alpha",
    "Environment :: GPU :: NVIDIA CUDA :: 12 :: 12.8",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
]


dependencies = [
    # Core System
    # These are the dependencies of the core scheduler
    "ray[default]>=2.47.0",
    "torch>=2.5.0",
    # dev8 rejects our version_base="1.1"; prerelease bound keeps agentic's dev1 pin valid
    "hydra-core<1.4.0.dev8",
    "numpy",

    # Data processing
    "datasets",
    "torchdata",
    "scipy",

    # Utilities
    "accelerate",
    "debugpy",
    "einops",
    "nvitop",
    "pybind11",
    "setuptools",
    "ninja",
    "pytest",
    "gsutil",
    "ruff",
    "pre-commit",
    "liger_kernel; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "uv",
    "pip",
    "huggingface_hub",
    "icmplib",
    # Codecs for `env.obs_compression` (Env -> Rollout observation transfer)
    "lz4",
    "zstandard",

    # Logging
    "wandb<0.25.1",
    "swanlab>=0.6.11",
    "tensorboard",

    "sortedcontainers"
]

[project.urls]
Homepage = "https://github.com/RLinf/RLinf"
Repository = "https://github.com/RLinf/RLinf"
Documentation = "https://rlinf.readthedocs.io/"
"Bug Report" = "https://github.com/RLinf/RLinf/issues"

[project.optional-dependencies]
agentic = [
    "numpy==2.2",
    # uvloop is not compatible with simulators, put it here
    "uvloop==0.21.0",
    "hydra-core==1.4.0.dev1",
    "omegaconf==2.4.0.dev4",
    "word2number",
    "regex",
    "peft==0.11.1",
    "rlinf_latex2sympy2",
    "mcp",
]
embodied = [
    "transformers<=4.57.6",
    "torchcodec",
    "peft",
    "timm",
    "imageio[ffmpeg]",
    "opencv-python",
    "gymnasium",
    "gym",
]
franka = [
    "pyyaml",
    "rospkg",
    "pyrealsense2; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "empy==3.3.4",
    "gymnasium==0.29.1",
    "pyquaternion",
    "opencv-python",
    "defusedxml",
    "pyspacemouse>=2.0.0",
    "pymodbus>=3.0,<4.0",
    "pyserial",
    "imageio[ffmpeg]",
    "evdev",
    "numpy<2",
    "pyzmq",
    "websockets"
]
xsquare_turtle2 = [
    "pyyaml",
    "rospkg",
    "pyrealsense2; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "toppra==0.6.3",
    "numpy==1.26.4",
    "empy==3.3.4",
    "gymnasium==0.29.1",
    "pyquaternion",
    "opencv-python",
    "defusedxml",
    "pyspacemouse>=2.0.0",
    "imageio[ffmpeg]",
    "evdev",
]
gim_arm = [
    "numpy",
    "scipy",
    "gymnasium",
    "opencv-python",
    "pyrealsense2; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "imageio[ffmpeg]",
]
so101 = [
    "numpy",
    "gymnasium",
    "opencv-python",
    "pyrealsense2; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "imageio[ffmpeg]",
]
piper = [
    "numpy",
    "scipy",
    "gymnasium",
    "opencv-python",
    "python-can>=3.3.4",
    "pyrealsense2; sys_platform == 'linux' and platform_machine == 'x86_64'",
    "imageio[ffmpeg]",
]

[tool.uv]
conflicts = [
    [
      { extra = "agentic" },
      { extra = "embodied" },
      { extra = "franka" },
      { extra = "xsquare_turtle2" },
      { extra = "gim_arm" }
    ],
]
override-dependencies = [
    "torch==2.11.0",
    "torchvision==0.26.0",
    "torchaudio==2.11.0",
    "torchcodec>=0.11,<0.12",
    "uvloop==0.21.0",
    "tensorflow-addons==0.23.0 ; platform_machine == 'x86_64'",
    "setuptools>=69.5.1,<75.9",
    "datasets==3.6.0",
    "pywin32==306; sys_platform == 'win32'",
    "sympy>=1.13.1",
    "tensorflow-metadata<=1.17.3",
    # Ray dashboard/agent loads googleapis-common-protos 1.75.1 (gencode 6.33.5).
    "protobuf>=6.33.5,<7",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["rlinf*"]
exclude = ["tests*", "docs*", "examples*", "docker*", "toolkits*"]

[tool.setuptools.package-data]
rlinf = ["**/*.yaml", "**/*.yml"]

[tool.ruff]
line-length = 88
indent-width = 4
target-version = "py311"

[tool.ruff.lint]
isort = {known-first-party = ["rlinf"]}
select = ["C", "E", "F", "I", "W", "CPY001", "RUF013", "UP006", "PERF102", "PLC1802", "PLC0208", "D", "RUF002", "DOC102", "DOC202", "DOC403", "DOC502", "ANN001", "ANN201", "ANN202", "ANN205", "ANN206"]
ignore = [
    "C901", # "complex-structure"
    "E501", # "line-too-long"
    "E741", # "ambiguous-variable-name"
    "D100", # "Missing docstring in public module"
    "D104", # "Missing docstring in public package"
    "D203", # "incorrect-blank-line-before-class", conflict with D211,
    "D213", # "multi-line-summary-second-line", conflict with D212
]
fixable = ["ALL"]
unfixable = []

[tool.ruff.lint.per-file-ignores]
# Only enable docstring check for the scheduler module for now.
"!rlinf/scheduler/**.py" = ["D", "RUF002"]
# Argument and return annotations are enforced where they are complete, so the
# gap cannot come back. Everything else is exempt until it is annotated too:
# turning ANN on repo-wide today would bury real findings under known ones.
"!{rlinf/robotics/**.py,rlinf/envs/real/**.py}" = ["ANN"]
# A test's fixtures are read next to their assertions, where a missing type
# costs nothing and the noise would be considerable.
"tests/**.py" = ["ANN"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
docstring-code-format = true
docstring-code-line-length = "dynamic"

