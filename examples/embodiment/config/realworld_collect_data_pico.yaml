defaults:
  - env/realworld_peg_insertion@env.eval
  - override hydra/job_logging: stdout

hydra:
  run:
    dir: .
  output_subdir: null
  searchpath:
    - file://${oc.env:EMBODIED_PATH}/config/

cluster:
  num_nodes: 1
  component_placement:
    env:
      node_group: franka
      placement: 0
  node_groups:
    - label: franka
      node_ranks: 0
      hardware:
        type: Franka
        configs:
          - robot_ip: ROBOT_IP    # Replace with your Franka IP
            node_rank: 0
            # "franka_ros" needs a ROS stack; "franky" talks libfranka.
            backend: franka_ros
            camera_serials: ["CAMERA_SERIAL_1", "CAMERA_SERIAL_2"]

runner:
  task_type: embodied
  logger:
    log_path: null
    project_name: rlinf
    experiment_name: "collect-data-pico"
    logger_backends: ["tensorboard"]
  num_data_episodes: 20
  record_task_description: False

env:
  group_name: "EnvGroup"
  eval:
    ignore_terminations: False
    auto_reset: False
    teleop: pico
    no_gripper: False
    pico:
      zmq_addr: "ipc:///tmp/vr_data.ipc"
      position_scale: 1.0
      rotation_scale: 1.0
      calibration:
        button: "trigger"
    max_episode_steps: 10000
    override_cfg:
      # Optional per-camera overrides keyed by RealSense serial number.
      # camera_names:
      #   "SERIAL1": global
      #   "SERIAL2": wrist_1
      # camera_crop_regions:
      #   "SERIAL1": [0.4, 0.3, 0.85, 0.7]
      target_ee_pose: TARGET_EE_POSE
      success_hold_steps: 1  # define how many consecutive steps the robot must hold the position to succeed

    data_collection:
      enabled: True
      save_dir: ${runner.logger.log_path}/collected_data
      export_format: "pickle"
      only_success: True
      robot_type: "panda"
      fps: 10
