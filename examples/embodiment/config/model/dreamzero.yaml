model_type: "dreamzero"
# DreamZero model directory (contains config.json, experiment_cfg/, and model.safetensors or sharded safetensors).
model_path: "/path/to/model/dreamzero"
tokenizer_path: "/path/to/models/umt5-xxl"

action_dim: 7
num_action_chunks: 16  # Action chunk size (libero_sim typically uses 16-32)

embodiment_tag: "libero_sim"
precision: "bf16"
